export interface ExperienceType {
  id: string;
  info: string[];
}

export interface ProjectType {
  id: string;
  title: string;
  description: string;
  url: string;
  imageUrl: string;
  repo: string;
  technologies: string[];
}